namespace AmbientCommon.Child
{
    public class ChildEnums
    {
        public enum Outcomes { EmotionalRefusal, QuietRefusal, EatsReluctantly, EatsEasily }
        public enum Actions { Distraction, LocationTable, LocationSofa, FoodReward, NonFoodReward, Pressure, ParentJoins }
        public enum States { High, Average, Low }
        public enum TalkTypes { FoodReward, NonFoodReward, Pressure, Modelling }
        public enum CustomisationCategory { HairColour, EyeColour }
        public enum Sex { Male, Female }
    }
}
